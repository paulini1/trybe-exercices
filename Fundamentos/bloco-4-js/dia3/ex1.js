let n=5; //Numero de vezes que vai se repetir.
let asterisco ="*"; //declara string e atribui valor *
for (let index =1 ; index <= n; index +=1){ //laço de repetição começando em 1.
    console.log(asterisco.repeat(n)) //console.log imprime variavel * , junto com a função repeat(n) que vai repetir 5 vezes.
}
